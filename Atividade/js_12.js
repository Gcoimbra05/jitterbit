// Declara uma variável com let (o valor pode mudar)
let idade = 25;

// Altera o valor da variável
idade = 26;

// Declara uma constante com const (o valor não pode ser alterado)
const pais = "Brasil";

// Exibe os valores no console
console.log(idade);
console.log(pais);